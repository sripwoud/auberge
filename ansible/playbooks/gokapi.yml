---
- name: Deploy Gokapi
  hosts: vps
  become: true
  roles:
    - role: gokapi
      tags: [gokapi]

  post_tasks:
    - name: Verify caddy ingress survived this deploy
      ansible.builtin.include_role:
        name: ingress_gate
        apply:
          tags: [always]
      tags: [always]
