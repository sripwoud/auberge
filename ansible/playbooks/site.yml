---
# Master playbook - Full stack deployment
# Composes all layers for complete VPS setup
#
# Usage:
#   Full deployment:     ansible-playbook site.yml
#   Skip bootstrap:      ansible-playbook site.yml --skip-tags bootstrap
#   Run specific layer:  ansible-playbook site.yml --tags hardening
#   Update apps only:    ansible-playbook site.yml --tags apps

- import_playbook: bootstrap.yml # noqa: name[play]
- import_playbook: hardening.yml # noqa: name[play]
- import_playbook: infrastructure.yml # noqa: name[play]
- import_playbook: auberge.yml # noqa: name[play]
