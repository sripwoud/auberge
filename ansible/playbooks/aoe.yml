---
- name: Deploy the agent-of-empires dashboard
  hosts: vps
  become: true
  roles:
    - role: aoe
      tags: [aoe]
      when: "'agent' in group_names"

  post_tasks:
    - name: Verify caddy ingress survived this deploy
      ansible.builtin.include_role:
        name: ingress_gate
        apply:
          tags: [always]
      tags: [always]
