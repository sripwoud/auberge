---
- name: Restart headscale
  ansible.builtin.systemd_service:
    name: headscale
    state: restarted
    daemon_reload: true

- name: Restart caddy
  ansible.builtin.systemd_service:
    name: caddy
    state: restarted
