---
- name: Bootstrap new VPS - Initial secure setup
  hosts: vps
  remote_user: "{{ bootstrap_user }}"
  gather_facts: true
  roles:
    - role: user
      tags: [user]

    - role: ssh
      tags: [ssh]
